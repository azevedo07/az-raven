# hooks/

React hooks customizados e reutilizáveis do Raven Studio.

Nenhum hook foi implementado ainda nesta etapa de fundação — este
diretório existe para receber a lógica de estado compartilhada (ex: dados
de projeto, sessão do usuário) que hoje vive localmente em
`components/providers/`, à medida que o projeto crescer.
